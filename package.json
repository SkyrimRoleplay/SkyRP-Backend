{
  "name": "frostfall-backend",
  "version": "1.0.0",
  "description": "Frostfall launcher backend API — serves news, server status, and client setup files",
  "main": "server.js",
  "scripts": {
    "start":    "node server.js",
    "dev":      "node --watch server.js",
    "setup":    "node scripts/setup-client.js",
    "merge":    "node scripts/merge-files.js",
    "populate": "node scripts/populate-files.js"
  },
  "dependencies": {
    "cors":    "^2.8.5",
    "express": "^4.19.0"
  }
}
